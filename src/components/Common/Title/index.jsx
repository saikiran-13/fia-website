import React from 'react'

const Title = ({ title }) => {
    return (
        <div className='flex w-full h-fit items-center justify-center'>
            <span className='text-3xl font-bold text-color5'>{title}</span>
        </div>

    )
}

export default Title